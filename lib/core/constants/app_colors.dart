import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales
  static const primary = Color(0xFF00798C);
  static const secondary = Color(0xFF1A1A1A);

  // Variations de la couleur principale (si nécessaire)
  static const Color primaryLight = Color(0xFF0AA5B6);
  static const Color primaryDark = Color(0xFF037180);

  // Couleurs communes
  static const Color grey = Colors.grey;
  static const Color white = Colors.white;
  static const Color black = Colors.black;

  // Couleurs de fond
  static const scaffoldBackground = Color(0xFFFAFAFA);
  static const cardBackground = Colors.white;
  static const imageBackground = Color(0xFFF5F5F5);
  
  // Couleurs de texte
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF757575);
  
  // Couleurs de bordure
  static const borderColor = Color(0xFFE0E0E0);
  
  // Couleurs d'état
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFF44336);
} 